"""{{ worker_title }} ({{ worker_id }}) — a Clonway fleet worker.

Scaffolded from clonway-cockpit's worker template (S8/C6): born with a cockpit,
a flag-guarded Signal emit path, a mandatory ``@scan_horizon`` stub, telemetry,
and the single write-gate + draft-never-send safety posture.
"""
