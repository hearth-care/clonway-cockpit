"""clonway-cockpit — the shared interactive-cockpit framework spine extracted
from xbook (C1). Provides the walk machine + write gate, the capability
registry, the render primitives, the key reader, local usage telemetry, the
shell-out mechanism, and the forward-looking Signal model. Workers depend on
this package and supply their own domain capabilities and screens."""
